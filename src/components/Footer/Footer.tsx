import React, { type ReactElement } from "react";

const Footer = (): ReactElement => {
  return (
    <div>
      <div className="m-5 bg-slate-200 bg-opacity-50 text-slate-600 text-lg h-60 font-bold text-center rounded-lg flex items-center justify-center">
        Footer
      </div>
    </div>
  );
};

export default Footer;
