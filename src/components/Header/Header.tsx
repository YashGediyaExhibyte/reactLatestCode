import React, { type ReactElement } from "react";

const Header = (): ReactElement => {
  return (
    <div>
      <div className="m-5 bg-slate-300 text-slate-600 text-lg py-5 font-bold text-center rounded-lg">
        Header
      </div>
    </div>
  );
};

export default Header;
